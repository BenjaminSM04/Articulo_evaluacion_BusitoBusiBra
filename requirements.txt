# Entorno científico auditado: Python 3.11.9, CUDA 12.1 (23-07-2026).
# Las versiones se fijan para que el reanálisis utilice la misma API y cálculo numérico.

# ---- Núcleo de deep learning ----
# Instala estas versiones con la build de CUDA adecuada a tu GPU:
#   https://pytorch.org/get-started/locally/
torch==2.5.1
torchvision==0.20.1
timm==1.0.27            # backbones preentrenados (ResNet-50, EfficientNet-B0, etc.)

# ---- Datos y procesamiento de imagen ----
numpy==1.26.4
pandas==3.0.3
pillow==12.2.0
opencv-python-headless==4.11.0.86
scikit-image==0.26.0
# GaussNoise usa std_range; el código convierte varianza de píxel mediante sqrt(var)/255.
albumentations==2.0.8
# Dependencias transitivas de albucore. La serie 5.x de stringzilla no publica
# wheel para CPython 3.11/Windows; se conservan las versiones auditadas en v2.
stringzilla==4.6.2
simsimd==6.5.16
ImageHash==4.3.2        # deduplicación perceptual (problema conocido de BUSI)

# ---- Métricas y validación ----
scikit-learn==1.9.0
scipy==1.17.1

# ---- Explicabilidad (XAI) ----
grad-cam==1.5.5         # paquete pytorch-grad-cam
shap==0.49.1
captum==0.9.0           # alternativa de atribuciones (IG, etc.)

# ---- Visualización y reportes ----
matplotlib==3.11.0
seaborn==0.13.2
tabulate==0.10.0         # requerido por pandas.DataFrame.to_markdown (reporte)
python-docx==1.2.0       # exportación de comparative_report.docx
reportlab==5.0.0         # exportación de comparative_report.pdf

# ---- Utilidades ----
pyyaml==6.0.3
tqdm==4.68.3
tensorboard==2.21.0
kaggle==2.2.3           # descarga de datasets desde Kaggle (requiere kaggle.json)
requests==2.34.2

# ---- Calidad de código y pruebas ----
pytest==9.1.1
black==26.5.1
ruff==0.15.20
